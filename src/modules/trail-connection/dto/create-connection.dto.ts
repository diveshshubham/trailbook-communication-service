// No DTO needed for now, but keeping for future extensibility
export class CreateConnectionDto {
  // Connection is created automatically when eligibility is met
  // This DTO can be used for future features like connection notes
}
